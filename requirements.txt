# =============================================================================
# DGA Parallel Detection - Python Dependencies
# AMLCCZG516 - ML System Optimization - Group 09 (Dissertation fork)
# =============================================================================
# Install: pip install -r requirements.txt
# Platform: Python 3.11.x on Windows 11 (HP OMEN 16 / AMD Ryzen 7 7840HS)
#
# Pins reconciled 2026-07-02 to a BASELINE-REPRODUCING environment. E7 under
# this set reproduces results/metrics.json BIT-EXACT for all 6 feature counts
# (5-feature = 93.1790%, 6-feature = 92.6020%). The only change vs the original
# coursework env is scikit-learn 1.4.2 -> 1.8.0: sklearn 1.6.x builds the RF
# differently on the fine-grained pronounceability feature and drops the
# 5-feature number to 92.24%; 1.8.0 matches the recorded baseline exactly.
# numpy/pandas are held at 2.4.6 / 2.2.3 (compat with other tooling; verified
# not to affect feature values or accuracy). Gate + 48/49 tests green.
# =============================================================================

# --- Core Scientific Computing ---
numpy==2.4.6
pandas==2.2.3
scipy==1.17.1

# --- Machine Learning ---
scikit-learn==1.8.0             # REQUIRED for baseline reproduction: 1.6.x drops the
                                # 5-feature accuracy to 92.24%; 1.8.0 reproduces 93.18%
                                # bit-exact (RF split behaviour on pronounceability).
joblib==1.5.3                   # Bundled with sklearn, pinned for reproducibility

# --- NLP / Linguistic Features ---
nltk==3.8.1                     # English dictionary corpus for meaningful word ratio
python-Levenshtein==0.27.3      # C-extension for the (dropped) Levenshtein feature;
                                # still imported by tests/ablation. Falls back to
                                # pure-Python DP if unavailable.

# --- Model Compression & ONNX Serving (RQ2) ---
marisa-trie==1.4.1              # Compact, mmap-backed DAWG for the dictionary.
                                # Also built by src/preprocess.py (build_english_dictionary),
                                # so it is a hard runtime dep, not RQ2-only.
onnx==1.22.0
onnxruntime==1.25.1             # Serving backend that drops the heavy sklearn import.
                                # PINNED DOWN from 1.27.0 (Batch-3 Step 2): ort >= 1.26.0
                                # has a TreeEnsembleClassifier kernel regression for
                                # binary RF - emits probabilities (-p1, +p1) instead of
                                # (1-p1, p1), which also corrupts the in-graph label
                                # (argmax always 1). 1.25.1 = newest version with exact
                                # parity (labels array_equal, probas within 1.2e-7).
skl2onnx==1.20.0                # sklearn -> ONNX conversion (zipmap=False; not implicated
                                # in the parity bug - 1.19.1 reproduced it identically).
treelite==4.7.0                 # Tree-model exchange + GTIL reference runtime (B3 probe 2)
tl2cgen==1.0.0                  # Native tree compiler - IMPORT-BLOCKED on this Windows
                                # layout (libloader expects a conda-style Library/bin) and
                                # needs MSVC; compiled-lib benchmark deferred to Batch 6.

# --- Serving API (RQ3) ---
fastapi==0.135.1                # Required by api.py (was previously unpinned/missing)
uvicorn==0.41.0                 # ASGI server for api.py

# --- System Monitoring & Profiling ---
psutil==7.2.2                   # CPU utilization, memory monitoring, per-core stats

# --- Visualization (P3 plots) ---
matplotlib==3.10.8
seaborn==0.13.2

# --- Testing ---
pytest==9.0.2
pytest-cov==5.0.0               # Coverage reporting

# --- Utilities ---
tqdm==4.67.3                    # Progress bars for long-running benchmarks
tldextract==5.1.2               # Robust TLD stripping (handles edge cases better
                                # than manual regex for .co.uk, .com.br, etc.)
