# =============================================================================
# DGA Parallel Detection - Python Dependencies
# AMLCCZG516 - ML System Optimization - Group 09
# =============================================================================
# Install: pip install -r requirements.txt
# Platform: Python 3.11.x on Windows 11 (HP OMEN 16 / AMD Ryzen 7 7840HS)
# =============================================================================

# --- Core Scientific Computing ---
numpy==1.26.4
pandas==2.2.1
scipy==1.13.0

# --- Machine Learning ---
scikit-learn==1.4.2
joblib==1.3.2                   # Bundled with sklearn, pinned for reproducibility

# --- NLP / Linguistic Features ---
nltk==3.8.1                     # English dictionary corpus for meaningful word ratio
python-Levenshtein==0.25.1      # Optimized C-extension for Levenshtein distance
                                # Falls back to pure-Python DP if unavailable

# --- System Monitoring & Profiling ---
psutil==5.9.8                   # CPU utilization, memory monitoring, per-core stats

# --- Visualization (P3 plots) ---
matplotlib==3.8.4
seaborn==0.13.2

# --- Testing ---
pytest==8.1.1
pytest-cov==5.0.0               # Coverage reporting

# --- Utilities ---
tqdm==4.66.2                    # Progress bars for long-running benchmarks
tldextract==5.1.2               # Robust TLD stripping (handles edge cases better
                                # than manual regex for .co.uk, .com.br, etc.)
